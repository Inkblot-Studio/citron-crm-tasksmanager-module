# Base URL for Jira API proxy calls.
# In production, set this to the deployment URL of this remote (e.g. https://citron-tasks-manager.vercel.app).
# Leave empty for local development with vercel dev or vite proxy.
VITE_API_BASE=
